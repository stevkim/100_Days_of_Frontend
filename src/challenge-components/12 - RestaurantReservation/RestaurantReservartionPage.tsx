import RestaurantCard from "./components/RestaurantCard";

const RestaurantReservationPage = () => {
  return (
    <section className="flex h-page w-full items-center justify-center">
      <RestaurantCard />
    </section>
  );
};

export default RestaurantReservationPage;
