export type TDOGGO = {
  id: number;
  name: string;
  image: string;
  breed: string;
  age: number; // in months
  weight: number; // in lbs
  height: number; // in inches
  location: string;
  description: string;
};
